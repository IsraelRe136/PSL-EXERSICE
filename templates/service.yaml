apiVersion: v1
kind: Service
metadata:
  name: {{ include "PSL-EXERSSICE.fullname" . }}
  labels:
    {{- include "PSL-EXERSSICE.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "PSL-EXERSSICE.selectorLabels" . | nindent 4 }}
