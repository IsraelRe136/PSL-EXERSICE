apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "PSL-EXERSSICE.fullname" . }}-test-connection"
  labels:
    {{- include "PSL-EXERSSICE.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "PSL-EXERSSICE.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
